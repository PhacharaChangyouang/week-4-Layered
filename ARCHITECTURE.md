# Week 4: Layered Architecture – Analysis & Comparison

---

## คำถามที่ 1: การเปรียบเทียบโครงสร้าง (5 คะแนน)

### ❓ Layered มีจำนวนไฟล์และบรรทัดโค้ดมากกว่าหรือน้อยกว่า Monolithic? เพราะอะไร?

**คำตอบ:**  
Layered Architecture มีจำนวนไฟล์และบรรทัดโค้ด **มากกว่า** Monolithic Architecture

**เหตุผล:**
- Layered แยกความรับผิดชอบออกเป็นหลายชั้น เช่น Controller, Service และ Repository
- โค้ดที่เคยรวมอยู่ในไฟล์เดียวของ Monolithic จะถูกแยกออกเป็นหลายไฟล์
- มี boilerplate เพิ่มขึ้นจากการเรียกใช้งานข้าม layer

ในขณะที่ **Monolithic Architecture**
- โค้ดส่วนใหญ่อยู่ในไฟล์เดียวหรือไม่กี่ไฟล์
- โครงสร้างเรียบง่าย เขียนเร็ว และไฟล์น้อย

---

### ❓ ความซับซ้อนที่เพิ่มขึ้นคุ้มค่าหรือไม่?

**คำตอบ:**  
ความซับซ้อนที่เพิ่มขึ้น **คุ้มค่า** ในกรณีที่โปรเจกต์มีขนาดกลางขึ้นไป หรือมีแนวโน้มจะขยายในอนาคต

- Layered ช่วยให้โค้ดดูแลรักษาง่าย
- สามารถแก้ไขเฉพาะส่วนโดยไม่กระทบทั้งระบบ
- เหมาะกับงานจริงและการทำงานเป็นทีม

อย่างไรก็ตาม สำหรับโปรเจกต์ขนาดเล็กมากหรือ prototype  
ความซับซ้อนนี้อาจ **ไม่คุ้มค่า** และ Monolithic จะเหมาะสมกว่า

---

## คำถามที่ 2: จุดแข็ง–จุดอ่อน (10 คะแนน)

| หัวข้อ | Monolithic (1–5) | Layered (1–5) | เหตุผล |
|------|------------------|---------------|--------|
| Maintainability | 2 | 5 | Layered แยกหน้าที่ชัดเจน ดูแลรักษาง่าย |
| Testability | 2 | 5 | Layered สามารถทดสอบแต่ละ layer แยกกันได้ |
| Modifiability | 2 | 5 | แก้ business logic หรือ database โดยไม่กระทบ UI |
| Reusability | 2 | 4 | Service / Repository นำกลับมาใช้ซ้ำได้ |
| Team Collaboration | 2 | 5 | ทีมสามารถแบ่งงานตาม layer ได้ชัดเจน |
| Performance | 5 | 4 | Monolithic เรียกใช้งานตรง ไม่มี overhead |
| Simplicity | 5 | 3 | Monolithic โครงสร้างง่ายกว่า |

**สรุป:**  
- Monolithic เด่นด้าน **ความง่ายและความเร็ว**
- Layered เด่นด้าน **คุณภาพโค้ดและการขยายระบบ**

---

## คำถามที่ 3: สถานการณ์จริง (5 คะแนน)

### 1️⃣ การเพิ่มฟีเจอร์ “assign task to user”

**Monolithic Architecture**
- ต้องแก้ไขหลายส่วนในไฟล์เดียว
- เสี่ยงกระทบ logic เดิม และโค้ดอ่านยากขึ้น

**Layered Architecture**
- เพิ่ม field ใน Repository
- เพิ่ม business rule ใน Service
- Controller ทำหน้าที่รับ–ส่งข้อมูลเท่านั้น  
👉 สามารถแก้ไขเป็นส่วน ๆ ได้อย่างปลอดภัย

---

### 2️⃣ บั๊กที่ validation logic (เช่น title เป็นค่าว่าง)

**Monolithic**
- validation อาจกระจายหลายจุด
- แก้ไม่ครบ → บั๊กยังคงอยู่

**Layered**
- validation อยู่ที่ Service เพียงจุดเดียว
- แก้ครั้งเดียว → ทุก API ได้รับผลพร้อมกัน

---

### 3️⃣ การเปลี่ยนฐานข้อมูลจาก SQLite เป็น PostgreSQL

**Monolithic**
- คำสั่ง SQL กระจายทั่วระบบ
- ต้องแก้หลายไฟล์ เสี่ยงเกิด error สูง

**Layered**
- แก้เฉพาะ Repository
- Service และ Controller ไม่ต้องเปลี่ยน

---

## คำถามที่ 4: Trade-offs (5 คะแนน)

### 🔹 Complexity vs Maintainability
- Layered มีความซับซ้อนด้านโครงสร้างมากกว่า
- แต่ช่วยให้ระบบดูแลรักษาและขยายได้ง่ายในระยะยาว
- เหมาะกับระบบที่ไม่ได้ “ทำส่งแล้วจบ”

👉 ยอมรับ complexity เพื่อแลกกับ maintainability

---

### 🔹 Performance Overhead
- Layered มี overhead จากการเรียกหลาย layer
- แต่ในระบบทั่วไป เช่น CRUD หรือ Web API
  overhead นี้แทบไม่ส่งผลอย่างมีนัยสำคัญ

👉 ประสิทธิภาพลดลงเล็กน้อย แต่ได้คุณภาพโค้ดที่ดีกว่า

---

## คำถามที่ 5: การตัดสินใจเลือกใช้ (5 คะแนน)

### 🧠 เกณฑ์การตัดสินใจเลือกสถาปัตยกรรม

**📌 ขนาดทีม**
- 1–2 คน → Monolithic (สื่อสารง่าย เขียนเร็ว)
- 3 คนขึ้นไป → Layered (แบ่งงานตาม layer)

**📌 ขนาดโปรเจกต์**
- เล็ก → Monolithic
- กลาง–ใหญ่ → Layered

**📌 ระยะเวลาพัฒนา**
- ต้องการเร็ว / Prototype → Monolithic
- มีเวลา / งานใช้งานจริง → Layered

**📌 ความต้องการ Maintainability**
- ต่ำ → Monolithic
- สูง → Layered
