คำถามที่ 1: การเปรียบเทียบโครงสร้าง (5 คะแนน)
❓ Layered มีจำนวนไฟล์และบรรทัดโค้ดมากกว่าหรือน้อยกว่า Monolithic? เพราะอะไร?

คำตอบ:
Layered Architecture มีจำนวนไฟล์และบรรทัดโค้ดมากกว่า Monolithic Architecture

เหตุผล:

Layered แยกความรับผิดชอบออกเป็นหลายชั้น (Controller, Service, Repository)

โค้ดที่เคยอยู่ไฟล์เดียวใน Monolithic จะถูกแยกออกเป็นหลายไฟล์

มี boilerplate เพิ่มขึ้น เช่น การเรียกข้าม layer

ในขณะที่ Monolithic:

โค้ดส่วนใหญ่รวมอยู่ไฟล์เดียวหรือไม่กี่ไฟล์

เขียนเร็ว ไฟล์น้อย โครงสร้างเรียบง่าย

❓ ความซับซ้อนที่เพิ่มขึ้นคุ้มค่าหรือไม่? อธิบาย

คำตอบ:
ความซับซ้อนที่เพิ่มขึ้น คุ้มค่าในกรณีที่โปรเจกต์มีขนาดกลางขึ้นไป หรือมีแนวโน้มจะขยายในอนาคต

Layered ช่วยให้โค้ดดูแลรักษาง่าย

แก้ไขเฉพาะส่วนโดยไม่กระทบทั้งระบบ

เหมาะกับงานจริงหรือการทำงานเป็นทีม

แต่สำหรับ โปรเจกต์เล็กมากหรือ prototype
ความซับซ้อนนี้อาจ ไม่คุ้มค่า และ Monolithic จะเหมาะสมกว่า

คำถามที่ 2: จุดแข็ง–จุดอ่อน (10 คะแนน)
หัวข้อ	Monolithic (1–5)	Layered (1–5)	เหตุผล
Maintainability	2	5	Layered แยกหน้าที่ชัด แก้ง่ายกว่า
Testability	2	5	Layered สามารถ test แต่ละ layer แยกกันได้
Modifiability	2	5	Layered แก้ logic หรือ DB โดยไม่กระทบ UI
Reusability	2	4	Service / Repository นำกลับมาใช้ซ้ำได้
Team Collaboration	2	5	ทีมแบ่งงานตาม layer ได้
Performance	5	4	Monolithic เรียกตรง ไม่มี overhead
Simplicity	5	3	Monolithic โครงสร้างง่ายกว่า

📌 สรุป:

Monolithic เด่นด้าน ความง่ายและความเร็ว

Layered เด่นด้าน คุณภาพโค้ดและการขยายระบบ

คำถามที่ 3: สถานการณ์จริง (5 คะแนน)
1️⃣ การเพิ่มฟีเจอร์ “assign task to user”

Monolithic:
ต้องแก้หลายจุดในไฟล์เดียว
เสี่ยงกระทบ logic เดิม และโค้ดเริ่มอ่านยาก

Layered:

เพิ่ม field ใน Repository

เพิ่ม business rule ใน Service

Controller แค่รับ/ส่งข้อมูล
👉 แก้เป็นส่วน ๆ ได้อย่างปลอดภัย

2️⃣ บั๊กที่ validation logic (เช่น title เป็นค่าว่าง)

Monolithic:
validation อาจกระจายหลายจุด
แก้ไม่ครบ → บั๊กยังอยู่

Layered:
validation อยู่ที่ Service ที่เดียว
แก้ครั้งเดียว → ทุก API ถูกแก้พร้อมกัน

3️⃣ การเปลี่ยนฐานข้อมูลจาก SQLite เป็น PostgreSQL

Monolithic:
SQL กระจายทั่วระบบ
ต้องแก้หลายไฟล์ เสี่ยงเกิด error สูง

Layered:
แก้เฉพาะ Repository
Service และ Controller ไม่ต้องเปลี่ยน

คำถามที่ 4: Trade-offs (5 คะแนน)
🔹 Complexity vs Maintainability

Layered มีความซับซ้อนด้านโครงสร้างมากกว่า

แต่ช่วยให้ระบบดูแลรักษาและขยายได้ง่ายในระยะยาว

เหมาะกับระบบที่ไม่ใช่แค่ “ทำส่งแล้วจบ”

👉 ยอมรับ complexity เพื่อแลก maintainability

🔹 Performance Overhead

Layered มี overhead จากการเรียกหลายชั้น

แต่ในระบบทั่วไป (CRUD / Web API)
overhead นี้ แทบไม่ส่งผลอย่างมีนัยสำคัญ

👉 ประสิทธิภาพลดลงเล็กน้อย แต่ได้คุณภาพโค้ดที่ดีกว่า

คำถามที่ 5: การตัดสินใจเลือกใช้ (5 คะแนน)
🧠 เกณฑ์การตัดสินใจเลือกสถาปัตยกรรม
📌 ขนาดทีม

1–2 คน: Monolithic (สื่อสารง่าย เขียนเร็ว)

3 คนขึ้นไป: Layered (แบ่งงานตาม layer)

📌 ขนาดโปรเจกต์

เล็ก: Monolithic

กลาง–ใหญ่: Layered

📌 ระยะเวลาพัฒนา

ต้องการเร็ว / prototype: Monolithic

มีเวลา / งานจริง: Layered

📌 ความต้องการ Maintainability

ต่ำ: Monolithic

สูง: Layered

